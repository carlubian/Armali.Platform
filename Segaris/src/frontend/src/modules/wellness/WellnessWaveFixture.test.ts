import { describe, expect, it } from 'vitest'

describe('wellness wave fixture', () => {
  it('keeps a shared frontend test location for later Wellness waves', () => {
    expect(true).toBe(true)
  })
})
