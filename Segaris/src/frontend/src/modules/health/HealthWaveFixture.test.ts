import { describe, expect, it } from 'vitest'

describe('health wave fixture', () => {
  it('keeps a shared frontend test location for later Health waves', () => {
    expect(true).toBe(true)
  })
})
