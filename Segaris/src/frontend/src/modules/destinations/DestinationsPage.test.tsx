import { describe, it } from 'vitest'

describe('DestinationsPage', () => {
  it.todo('renders the destination gallery when Wave 6 adds the page')
})

describe('DestinationPlacesPage', () => {
  it.todo('renders destination-scoped places when Wave 7 adds the page')
})
