# Infrastructure-only stack for native backend development.
#
# Runs just the backing services and publishes their ports to localhost so the
# backend and tests can run natively (dotnet run / dotnet test) against them:
#
#   docker compose -f docker-compose.infra.yml up -d
#
# (scripts/infra-up.ps1 wraps this.) Point the native backend at this PostgreSQL
# through CONNECTIONSTRINGS__BLACKWING or appsettings.json, for example:
#   Host=localhost;Port=5432;Database=blackwing;Username=blackwing;Password=blackwing
#
# Seq is optional and only started with the "seq" profile:
#   docker compose -f docker-compose.infra.yml --profile seq up -d

name: blackwing-infra

services:
  postgres:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${BLACKWING_POSTGRES_DB:-blackwing}
      POSTGRES_USER: ${BLACKWING_POSTGRES_USER:-blackwing}
      POSTGRES_PASSWORD: ${BLACKWING_POSTGRES_PASSWORD:-blackwing}
    ports:
      - "${BLACKWING_POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${BLACKWING_POSTGRES_USER:-blackwing} -d ${BLACKWING_POSTGRES_DB:-blackwing}"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  seq:
    image: datalust/seq:latest
    profiles:
      - seq
    restart: unless-stopped
    environment:
      ACCEPT_EULA: "Y"
    ports:
      # Seq web UI.
      - "${BLACKWING_SEQ_UI_PORT:-5341}:80"
    volumes:
      - seq-data:/data

volumes:
  postgres-data:
  seq-data:
