import { ClipboardCheck, Image, Settings, UploadCloud } from 'lucide-react'
import { BrowserRouter, Navigate, NavLink, Route, Routes } from 'react-router-dom'
import { GalleryPage, ReviewPage, UploadPage } from '@/features/pages'
const navigation = [{ to: '/gallery', label: 'Gallery', icon: Image }, { to: '/review', label: 'Pending review', icon: ClipboardCheck }, { to: '/upload', label: 'Upload', icon: UploadCloud }]
export function App() { return <BrowserRouter><div className="application-shell"><aside className="sidebar"><div className="brand"><span className="brand-mark">B</span><span>Blackwing</span></div><nav>{navigation.map(({ to, label, icon: Icon }) => <NavLink key={to} to={to} className={({ isActive }) => isActive ? 'nav-link active' : 'nav-link'}><Icon size={18} />{label}</NavLink>)}</nav><div className="sidebar-footer"><Settings size={17} />Foundation · Phase 1</div></aside><main className="main-content"><Routes><Route path="/gallery" element={<GalleryPage />} /><Route path="/review" element={<ReviewPage />} /><Route path="/upload" element={<UploadPage />} /><Route path="*" element={<Navigate to="/gallery" replace />} /></Routes></main></div></BrowserRouter> }
