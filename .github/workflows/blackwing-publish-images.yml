name: Blackwing Publish Images

on:
  workflow_run:
    workflows:
      - Blackwing Foundation Validation
    branches:
      - main
    types:
      - completed
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

concurrency:
  group: blackwing-publish-images-${{ github.event.workflow_run.head_sha || github.sha }}
  cancel-in-progress: false

jobs:
  publish:
    name: Publish Blackwing ${{ matrix.image.name }}
    if: >-
      (github.event_name == 'workflow_run' &&
       github.event.workflow_run.event == 'push' &&
       github.event.workflow_run.conclusion == 'success') ||
      (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')
    runs-on: ubuntu-latest
    timeout-minutes: 45
    environment: segaris-production-images
    strategy:
      fail-fast: false
      matrix:
        image:
          - name: backend
            repository: blackwing-backend
            context: Blackwing
            dockerfile: Blackwing/src/backend/Blackwing.Api/Dockerfile
          - name: frontend
            repository: blackwing-frontend
            context: Blackwing/src/frontend
            dockerfile: Blackwing/src/frontend/Dockerfile

    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}
          persist-credentials: false

      - name: Sign in to Azure with OIDC
        uses: azure/login@v3
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Sign in to Azure Container Registry
        run: az acr login --name "${{ vars.ACR_NAME }}"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build and push immutable image
        uses: docker/build-push-action@v7
        with:
          context: ${{ matrix.image.context }}
          file: ${{ matrix.image.dockerfile }}
          push: true
          tags: ${{ vars.ACR_LOGIN_SERVER }}/${{ matrix.image.repository }}:${{ github.event.workflow_run.head_sha || github.sha }}
          cache-from: type=gha,scope=blackwing-${{ matrix.image.name }}
          cache-to: type=gha,mode=max,scope=blackwing-${{ matrix.image.name }}
          provenance: true
          sbom: true
